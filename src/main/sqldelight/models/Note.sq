CREATE TABLE Note (
    id INTEGER NOT NULL PRIMARY KEY AUTOINCREMENT,
    title TEXT NOT NULL
);

insert: INSERT INTO Note(title) VALUES (?);

selectAll: SELECT * FROM Note;

selectNote: SELECT * FROM Note WHERE id = ?;

deleteNote: DELETE FROM Note WHERE id = ?;